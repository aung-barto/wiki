<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title><%= info.title %></title>
	<nav>
	<a href="/wiki">Home</a>
	<a href="/article/new">Create New Article</a>
	<a href="/user/new">Create New User</a>
	</nav>
	<h1>Wikifoodies</h1>
	<style>
	img {
		height:375px;
		width: auto;
	}
	#author{
		display: inline;
	}
	.edit {
		text-align: right;
		padding-right: 50px;
	}
	body{
		padding-left: 50px;
	}
	.content{
		text-align: left;
	}
	img{
		float: right;
		padding: 10px 50px 10px 10px;
	}
	#footer{
		display: inline;
	}
	</style>
</head>
<body>
	<h2><%= info.title %></h2><br>

		<div id="author">
			<a href="/user/<%= info.id %>"><%= info.name %></a>
			<a href="/article/<%=articles.id %>/history"><%= articles.create_at %></a>
		</div>
		<div class="edit">
			<a href="/article/<%= articles.id %>/edit">Edit Article</a>
		</div>
		
		<img src="<%= articles.image %>">
		<%- content %>
		
		<div id="footer">
		<form method="POST" action="/article/<%= articles.id %>">
		<label>Choose Your Username</label>
			<select name="username">
			<% allUsers.forEach(function(name){ %>
				<option value="<%= name.id %>"><%= name.name %></option>
				<% }); %>
			</select>
			<button>Subscribe To This Article</button>
		</form>
		
		<form method="POST" action="/article/<%=articles.id %>?_method=DELETE">
			<input type="checkbox" name="delete" value="delete" required>Confirm</input>
		<button>Delete Article</button>
		</form>
		</div>
</body>
</html>